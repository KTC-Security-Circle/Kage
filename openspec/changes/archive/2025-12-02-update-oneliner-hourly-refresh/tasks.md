## 1. Implementation

- [ ] 1.1 `OneLinerApplicationService` に 1 時間 TTL のキャッシュ用フィールドと `_now()` ヘルパーを追加し、クエリ省略時に有効期限を確認して `OneLinerAgent` 呼び出しを省略する。
- [ ] 1.2 自動コンテキスト構築時に生成したメッセージをキャッシュへ保存し、TTL 経過後にのみ再生成するようにする。明示クエリ経路は必ずエージェントを実行するよう条件分岐を追加する。
- [ ] 1.3 例外やエージェント異常でデフォルトメッセージへフォールバックした場合でも、望ましい振る舞い（例: 失敗時は再試行を許すかどうか）を定義し、必要ならキャッシュをスキップ／更新する分岐を実装する。

## 2. Testing

- [ ] 2.1 `test_one_liner_application_service.py` にキャッシュ命中・TTL 失効・明示クエリバイパスを検証するユニットテストを追加し、エージェント呼び出し回数と戻り値の一貫性を確認する。
