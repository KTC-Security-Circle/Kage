"""Command modules register Typer sub-commands here.

Import each command module so that its decorators execute and register the
commands with the root Typer application. Keep imports explicit for clarity.
"""
